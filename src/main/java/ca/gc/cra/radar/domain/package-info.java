/**
 * Domain models and invariants for RADAR capture, flows, messages, and protocols.
 * <p>Contains immutable value types for segments, byte streams, protocol identifiers,
 * and message metadata. No infrastructure dependencies.</p>
 *
 * @since RADAR 0.1-doc
 */
package ca.gc.cra.radar.domain;
